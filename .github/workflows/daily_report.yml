name: Daily AI Idea Research Report

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 2 * * *'  # 9:00 AM VN Time (02:00 UTC)
    - cron: '0 7 * * *'  # 2:00 PM VN Time (07:00 UTC)
    # ===== [TEST TEMPORARY SCHEDULE FOR TODAY 2026-07-23] =====
    - cron: '30 4 23 7 *' # TEST: 11:30 AM VN Time today (04:30 UTC)
    - cron: '0 5 23 7 *'  # TEST: 12:00 PM VN Time today (05:00 UTC)
    # ==========================================================
  workflow_dispatch:      # Allows manual trigger

jobs:
  run-report:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install requests feedparser beautifulsoup4 google-genai pytest pytest-mock

      - name: Run unit tests
        env:
          PYTHONPATH: .
        run: |
          pytest tests/

      - name: Run research script
        env:
          PYTHONPATH: .
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python src/main.py
